{
  "type": "object",
  "properties": {
    "protoSrc": {
      "type": "string",
      "description": "The source directory of the protobuf files."
    },
    "output": {
      "type": "string",
      "description": "The output directory for the compiled files."
    }
  },
  "required": ["protoSrc", "output"]
}
